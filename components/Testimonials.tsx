import { testimonials } from "@/content/testimonials";

// Placeholder until step 07 builds the carousel. Returns null when there is no
// real data, which is the production state until students report scores.
export function Testimonials() {
  if (testimonials.length === 0) return null;
  return null;
}
